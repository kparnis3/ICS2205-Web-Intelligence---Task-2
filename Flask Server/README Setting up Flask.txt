Instructions to setup Flask Server

open windows cmd
cd to Flask Server folder
write command "set FLASK_APP=Flask.py"
write command "python -m flask run"
finally, copy address given by the cmd and paste into google.

